[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npx prisma migrate deploy --skip-generate && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
